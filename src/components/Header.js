export default function Header() {
  return (
    <h1
      style={{
        display: "flex",
        justifyContent: "center",
        fontSize: "2.5em",
        margin: "40px",
        fontFamily: " Georgia, serif",
      }}
    >
      THE NEWS APP
    </h1>
  );
}
